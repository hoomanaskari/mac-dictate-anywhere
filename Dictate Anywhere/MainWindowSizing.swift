//
//  MainWindowSizing.swift
//  Dictate Anywhere
//
//  Shared sizing constants for the main app window.
//

import CoreGraphics

enum MainWindowSizing {
    static let minimumWidth: CGFloat = 680
    static let minimumHeight: CGFloat = 560
    static let maximumWidth: CGFloat = minimumWidth * 2
    static let maximumHeight: CGFloat = minimumHeight * 2
}
